{
  "buildCommand": "cd client && rm -rf dist && npm run build && echo 'Build complete' && ls -la dist/",
  "outputDirectory": "client/dist",
  "installCommand": "cd server && npm install && cd ../client && npm install --no-audit --no-fund",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  }
}
